// Define a constant for the application name prefix
const appName = 'notesApp_';

// Export an object with keys, where 'notes' is a concatenation of the appName and 'Notes'
export const keys = {
  notes: `${appName}Notes`,
};
